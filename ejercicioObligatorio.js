"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var rls = require("readline-sync");
var base = rls.questionInt("Ingrese la base numerica: ");
var exponente = rls.questionInt("Ingrese un exponente numerico: ");
var resultadoPotencia = 0;
function calcularPotencia(base, exponente) {
    var resultado = 0;
    if (exponente < 0) {
        console.log("El exponente ingresado no es valido porque es menor a cero");
        if (resultado = Math.pow(base, exponente))
            ;
    }
}
return resultadoPotencia;
resultadoPotencia = calcularPotencia(base, exponente);
